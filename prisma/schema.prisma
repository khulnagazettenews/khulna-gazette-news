datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
}

// Roles: "SUPER_ADMIN", "EDITOR", "REPORTER"
model User {
  id        String   @id @default(uuid()) @map("_id")
  name      String
  email     String   @unique
  password  String
  role      String   @default("REPORTER") // Mapped from Enum Role
  avatar    String?
  bio       String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  news      News[]
}

model Category {
  id            String     @id @default(uuid()) @map("_id")
  name          String
  slug          String     @unique
  parentId      String?
  parent        Category?  @relation("SubCategories", fields: [parentId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  subCategories Category[] @relation("SubCategories")
  order         Int        @default(0)
  createdAt     DateTime   @default(now())
  updatedAt     DateTime   @updatedAt
  
  // Relations for News
  news          News[]     @relation("CategoryNews")
  subNews       News[]     @relation("SubCategoryNews")
}

model Tag {
  id        String   @id @default(uuid()) @map("_id")
  name      String   @unique
  slug      String   @unique
  newsIds   String[]
  news      News[]   @relation("NewsTags", fields: [newsIds], references: [id])
}

model News {
  id              String      @id @default(uuid()) @map("_id")
  title           String
  subtitle        String?
  slug            String      @unique
  content         String      
  featuredImage   String?
  imageCaption    String?
  photoCredit     String?
  
  categoryId      String
  category        Category    @relation("CategoryNews", fields: [categoryId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  
  subCategoryId   String?
  subCategory     Category?   @relation("SubCategoryNews", fields: [subCategoryId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  
  authorId        String
  author          User        @relation(fields: [authorId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  
  reporterName    String?     
  authorTitle     String?
  tagIds          String[]
  tags            Tag[]       @relation("NewsTags", fields: [tagIds], references: [id])
  
  status          String      @default("DRAFT") // Mapped from Enum: DRAFT, PUBLISHED, SCHEDULED, TRASHED
  isBreaking      Boolean     @default(false)
  isFeatured      Boolean     @default(false)
  
  publishedAt     DateTime?
  scheduledAt     DateTime?
  viewCount       Int         @default(0)
  order           Int         @default(0)
  
  metaTitle       String?
  metaDescription String?
  
  createdAt       DateTime    @default(now())
  updatedAt       DateTime    @updatedAt
  
  comments        Comment[]
}

model EpaperIssue {
  id        String   @id @default(uuid()) @map("_id")
  date      DateTime @unique
  pdfUrl    String?
  imageUrl  String?
  imageUrls String[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model GalleryPhoto {
  id        String   @id @default(uuid()) @map("_id")
  imageUrl  String
  caption   String?
  credit    String?
  order     Int      @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model GalleryVideo {
  id          String   @id @default(uuid()) @map("_id")
  youtubeUrl  String
  title       String
  categoryTag String?
  description String?
  order       Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PrayerTime {
  id        String   @id @default(uuid()) @map("_id")
  date      DateTime @unique
  fajr      String
  zohr      String
  asr       String
  magrib    String
  esha      String
  sunrise   String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Comment {
  id        String   @id @default(uuid()) @map("_id")
  newsId    String
  news      News     @relation(fields: [newsId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  name      String
  comment   String
  approved  Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Advertisement {
  id          String    @id @default(uuid()) @map("_id")
  title       String
  imageUrl    String
  targetUrl   String?
  position    String    // e.g. "top_banner", "sidebar_banner", "sidebar_widget_top", "sidebar_widget_middle", "sidebar_widget_bottom"
  adType      String    @default("IMAGE") // IMAGE, HTML_SCRIPT, TEXT_IMAGE
  codeSnippet String?   // Custom HTML / JS AdSense code
  description String?   // Text description for TEXT_IMAGE ads
  order       Int       @default(0)
  status      String    @default("ACTIVE") // ACTIVE, INACTIVE
  startDate   DateTime?
  endDate     DateTime?
  clicks      Int       @default(0)
  views       Int       @default(0)
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}

model SpecialTopic {
  id             String   @id @default(uuid()) @map("_id")
  title          String
  bannerSubtitle String?
  isActive       Boolean  @default(true)
  newsIds        String[]
  order          Int      @default(0)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}

model TopNews {
  id        String   @id @default("top_news") @map("_id")
  newsIds   String[]
  updatedAt DateTime @updatedAt
}

model JobApplication {
  id               String   @id @default(uuid()) @map("_id")
  position         String
  fullName         String
  dob              String?
  mobile           String
  email            String
  facebookLink     String?
  presentAddress   String?
  permanentAddress String?
  educations       String?
  currentJobs      String?
  experiences      String?
  workLinks        String?
  photoUrl         String?
  nidUrl           String?
  notes            String?
  status           String   @default("PENDING")
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
}

model NavbarMenu {
  id        String       @id @default(uuid()) @map("_id")
  name      String
  url       String
  parentId  String?
  parent    NavbarMenu?  @relation("SubMenuItems", fields: [parentId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  subItems  NavbarMenu[] @relation("SubMenuItems")
  order     Int          @default(0)
  createdAt DateTime     @default(now())
  updatedAt DateTime     @updatedAt
}


